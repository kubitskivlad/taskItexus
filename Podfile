# platform :ios, '12.1'

target 'normTask' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for taskItexus
  pod 'RxSwift', '~> 4.0'
  pod 'RxCocoa', '~> 4.0'
  pod 'RxDataSources', '~> 3.0'

  # Community projects
  pod 'Action', '~> 3.4'
  pod 'NSObject+Rx', '~> 4.1'
  
  # Realm database
  pod 'RealmSwift', '~> 3.0'
  pod 'RxRealm', '~> 0.7'

  target 'normTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'normTaskUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
